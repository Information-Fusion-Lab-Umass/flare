# Experiment
exp_id: debug_cnn # base_T_1_onehot_2 #base_T_1_onehot
output_dir: &output ../outputs

# Data
data:
    path_load: ../data/data_nonan.pickle
    path_meta: ../data/metadata
    path_images: ../../../ADNI_data/data
    path_feat: ../data/features.csv
    min_visits: 2

datagen:
    data_split: &data_split 0.8
    batch_size: &bsize 4
    num_visits: 0
    feat_flag: &imgType cnn3d #tadpole 

# Model 
model:
    load_model: ''
    module_image:
        name: *imgType # tadpole1
    module_temporal: 
        name: rnn
    module_forecast:
        name: append_time
    module_task:
        name: dx
    fusion: latefuse

# Train           
train:
    num_epochs: 1
    save_model: True

# Test
test:
    task: classify
    data_split: *data_split
    batch_size: *bsize
    feat_flag: *imgType

